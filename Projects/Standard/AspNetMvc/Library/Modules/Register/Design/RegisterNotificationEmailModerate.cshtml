@using OnWeb.Core.DB
@model User

Уважаемый посетитель!<br />

@{ var credentitals = string.Join(" или ", new string[] { !string.IsNullOrEmpty(Model.email) ? "адрес электронной почты" : string.Empty, !string.IsNullOrEmpty(Model.phone) ? "номер телефона" : string.Empty }.Where(x => !string.IsNullOrEmpty(x))); }
Ваша заявка на регистрацию получена. Администратор рассмотрит Вашу заявку и примет решение, после чего Вы получите уведомление на @credentitals.
Для авторизации Вы можете использовать @credentitals и пароль, указанные при регистрации.<br /><br />

@if (!string.IsNullOrEmpty(Model.email))
{<text>Адрес электронной почты - @Model.email.<br /></text>}

@if (!string.IsNullOrEmpty(Model.phone))
{<text>Номер телефона - @Model.phone.<br /></text>}
<br />

<br />

Спасибо за внимание!<br />
Почтовый робот сайта @Url.ContentFullPath("")
