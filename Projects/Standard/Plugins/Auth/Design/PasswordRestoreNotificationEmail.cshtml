@model Auth.Design.Model.PasswordRestoreSend
@{
    var location = "/" + Module.Name + "/restore3/?Code=" + Model.Code;
    if (!string.IsNullOrEmpty(Model.CodeType)) { location += "&CodeType=" + Model.CodeType; }
}

Здравствуйте, @Model.User.name!<br />
Вы (или кто-то от Вашего имени) запросили восстановление пароля на сайте @Url.ContentFullPath("")<br /><br />

Для восстановления пароля перейдите по ссылке и следуйте инструкциям:<br />
@Url.ContentFullPath(location)<br /><br />

Либо Вы можете ввести код подтверждения, указанный ниже, вручную:<br />
Код подтверждения: @Model.Code<br /><br />

Ваши данные:<br />
Email: @Model.User.email<br /><br />

С уважением, редакция сайта<br />
@Url.ContentFullPath("")<br />
